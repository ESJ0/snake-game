version: '3.9'

services:
  snake-game:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: snake-game
    ports:
      - "${APP_PORT:-3000}:80"
    environment:
      - NODE_ENV=${NODE_ENV:-production}
    restart: unless-stopped
